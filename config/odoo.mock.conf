; ─────────────────────────────────────────────────────────────────────────────
; PUBLIC / ACADEMIC mode — mock + synthetic ONLY.
; addons_path does NOT include any private path. This is the config reviewers use.
; ─────────────────────────────────────────────────────────────────────────────
[options]
; 1) Odoo core addons   2) public PG-Agent addons (mock + guard)
addons_path = /opt/odoo/addons,/opt/odoo/odoo/addons,./addons
db_name = authzbench
admin_passwd =
xmlrpc_port = 8069
list_db = True
log_level = info
; No private submodule. pco_core_mock provides the schema skeleton.
